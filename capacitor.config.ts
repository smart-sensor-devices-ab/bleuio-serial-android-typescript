import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.bleuio', // Change to your app's bundle ID
  appName: 'BleuIO Serial',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
